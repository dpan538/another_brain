# R27A6 Optional Teacher Distillation

Live teacher use remains disabled by default and requires both `--execute-live-teacher` and `R27A6_ALLOW_LIVE_TEACHER=1`. No credentials were committed. In the default local run, status is `blocked_no_credentials` and zero teacher rows are promoted. Teacher candidates, if ever produced, are training-time pending candidates only and are not product runtime dependencies.
