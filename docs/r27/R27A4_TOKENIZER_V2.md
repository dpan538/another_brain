# R27A4 Tokenizer V2

Tokenizer type: `chinese_aware_bpe`. Vocab size: `16000`. It is trained only on `artifacts/r27a4/training_mix/interleaved_train.jsonl`, not heldout. Chinese fertility: `0.5581`.

Tokenizer artifacts remain ignored under `artifacts/r27a4/model_lab/tokenizer/`.
