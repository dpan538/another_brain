# R27B1B Non-Claims

R27B1B does not claim:

- product model
- real model admission
- browser model admission
- release checkpoint
- backend inference
- external LLM API
- Doubao integration
- hosted vector store
- committed weights, tokenizer artifacts, exported assets, ONNX files, or quantized shards

The default path is mock or synthetic runtime only. Browser smoke is not quality evidence.
