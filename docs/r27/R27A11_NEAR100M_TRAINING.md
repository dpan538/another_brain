# R27A11 Near-100M Training

R27A11 may train only after loss-accounting validation, full static budget selection, scale smoke, and resource guard all pass.

The campaign id is `r27a11_near100m_budgetfit_candidate_v1`. It is an engineering candidate run only, not product training, not formal decoder training, not phase_4, not browser admission, and not a release checkpoint.

The primary train-token metric is `optimizer_tokens`. `last_batch_loss` is debug only.
