export const WEB_SLM_PROFILE = Object.freeze({
  id: "tiny-router-web-slm",
  modelId: "tiny-router-web-slm",
  label: "Another Brain tiny router Web SLM",
  kind: "route-and-answer",
  estimatedVramMb: 0,
  lowResource: true,
  requiresWebGpu: false,
  requiresNetwork: false
});

// Kept as a compatibility alias for older gate scripts.
export const LOCAL_MODEL = WEB_SLM_PROFILE;
